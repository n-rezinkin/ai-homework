# Состояние проекта

**Обновлено:** 2026-08-17, сессия 7

## Готово

- **Правила.** `AGENTS.md`, Часть II заполнена под задачу: стек, команды, запреты
  категоричными формулировками, случаи остановки, чек-лист перед «готово».
  Часть I не тронута.
- **Спецификация.** `SPEC.md` написан до кода, проверен независимой сессией
  (14 находок, все приняты, вердикты внутри), дополнен по итогам.
- **Наивный прогон.** `naive/habit-tracker.html`, 431 строка, не правился.
- **Приложение.** `app/` — Vite 8 + React 19 + TypeScript 6. Три состояния,
  недельная сетка, серии, импорт и экспорт, темы, клавиатурная навигация
  стрелками, фокус-трап в оверлее, адаптив с перестроением в карточку.
- **Дизайн-проход.** Отдельной сессией через скилл `frontend-design`,
  логика не тронута (подтверждено диффом).
- **Проверка.** Оба прогона по одному чек-листу: по спеке 30/30,
  наивный 9/13. Выводы в `evidence/checklist-*.md`, скриншоты в
  `evidence/before/`, `evidence/after/`, `evidence/naive/`.
- **README.** Запуск, что сделано, что упрощено и не проверено, сравнение
  двух прогонов с конкретикой.
- **Журнал.** Семь файлов сессий, `TOOLS.md` с шестью записями.
- **Аудит кода.** Независимая сессия в режиме `plan`, 9 находок, все приняты
  и исправлены. Отчёт — `evidence/code-audit.md`, вердикты — `sessions/session-6.md`.
- **Финальная проверка.** 30 из 30, линтер и сборка чистые.
- **Контраст.** Замерен по WCAG в обеих темах: минимум 7.09:1 и 7.23:1,
  уровень AAA. Отчёт — `evidence/contrast.md`.

## В работе

Ничего. Все семь сессий закрыты.

## Не начато

- Публикация в репозиторий и отправка ссылки. Git в проекте не ведётся
  по решению автора — папка отдаётся как есть.

## Активные инструменты

- Claude Code 2.1.205, Opus 5, режим «одобрение правок».
- Скилл `frontend-design` (использован в сессии 4).
- Playwright + Chromium — вне репозитория, для проверок и скриншотов.
- `python3 -m http.server 8765` — для наивного прогона.
- pypdf — разово, чтение `homework.pdf`.

## Известные проблемы

- **Автотестов в проекте нет.** Чек-лист живёт вне репозитория, в репозиторий
  попал только вывод. В живом проекте так нельзя.
- **Нет прогона под другим часовым поясом.** Ключи дат строятся из локальных
  компонент и `toISOString` запрещён явно, но это проверено чтением кода,
  а не запуском.
- **Синхронизация между вкладками не сделана** — две вкладки затрут друг друга.
- **Git-истории нет**, порядок работы подтверждается только журналом.
- **Порт 5173 был занят** чужим процессом, работа шла на 5174. В README оговорено.

## Следующий шаг

Работа завершена. Осталось действие человека: прочитать `SPEC.md`, Часть II
`AGENTS.md` и `README.md` глазами, после чего выложить папку в репозиторий
и отправить ссылку.
